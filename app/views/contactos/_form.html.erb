<%= form_for(@contacto) do |f| %>
    <% if @contacto.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@contacto.errors.count, "error") %> prohibited this contacto from being saved:</h2>

          <ul>
            <% @contacto.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <%= f.label :nombre %>
      <br/>
      <%= f.text_field :nombre, :placeholder => "nombre", :autofocus => "true", :required => "true" %>
    </div>
    <div class="field">
      <%= f.label :email %>
      <br/>
      <%= f.text_field :email, :placeholder => "correo electrónico", :autofocus => "true", :required => "true" %>
    </div>
    <div class="field">
      <%= f.label :asunto %>
      <br/>
      <%= f.text_field :asunto, :placeholder => "asunto", :required => "true" %>
    </div>
    <div class="field">
      <%= f.label :mensaje %>
      <br/>
      <%= f.text_area :mensaje, :placeholder => "mensaje", :required => "true", :rows => "3" %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
